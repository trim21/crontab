name: pre-commit

on:
  workflow_dispatch:
  push:
    branches:
      - "master"
    paths:
      - ".github/workflows/pre-commit.yaml"
      - "pre_commit.mjs"
      - "common/version.sh"
      - package.json
      - yarn.lock
  schedule:
    # daily on github default branch
    - cron: "0 0 15 */2 *"
  repository_dispatch:
    types:
      - pre-commit
jobs:
  main:
    strategy:
      fail-fast: false
      matrix:
        include:
          - { repo: BGmi/BGmi, branch: master, py: "3.10" }
          # - { repo: Trim21/pol, branch: master, py: "3.8" }
          # - { repo: trim21/pre-commit-hooks, branch: master, py: "3.10" }
          - { repo: trim21/transmission-rpc, branch: master, py: "3.10" }
          - { repo: bangumi/service-timeline, branch: master }
          - { repo: BGmi/anime-episode-parser, branch: master, py: "3.10" }
          - { repo: "trim21/flexget-plugins", branch: master, py: "3.10", prettier: true }

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      ################ node

      - uses: actions/setup-node@v3
        with:
          node-version: "lts/*"
          registry-url: "https://registry.npmjs.org"
          cache: yarn

      - run: yarn
      ################ end node

      ################ python

      - uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.py }}

      - name: Get Latest tools version
        id: get-version
        run: bash ./common/version.sh

      - uses: Trim21/setup-poetry@dist/v1

      - uses: actions/checkout@v3
        with:
          repository: ${{ matrix.repo }}
          ref: ${{ matrix.branch }}
          path: ${{ matrix.repo }}
          token: ${{ secrets.PERSONAL_GITHUB_TOKEN }}

      - name: Fetch All Branches
        working-directory: ./${{ matrix.repo }}
        run: git fetch --no-tags --prune --depth=1 origin +refs/heads/*:refs/remotes/origin/*

      - uses: actions/cache@v3
        name: Cache Python Packages
        with:
          path: ~/.local
          key: poetry-${{ matrix.repo }}-${{ env.PY }}-${{ hashFiles('**/poetry.lock') }}
          restore-keys: poetry-${{ matrix.repo }}-${{ env.PY }}-

      - uses: actions/cache@v3
        name: Cache Pre-Commit
        with:
          path: ~/.cache/pre-commit
          key: pre-commit-${{ matrix.repo }}-${{ env.PY }}-${{ hashFiles('**/.pre-commit-config.yaml') }}
          restore-keys: pre-commit-${{ matrix.repo }}-${{ env.PY }}-

      - name: Install Dependencies
        working-directory: ./${{ matrix.repo }}
        run: pip install --user --no-deps -r <(poetry export -f requirements.txt --with dev)

      - name: check if pre-commit update
        run: node $GITHUB_WORKSPACE/pre_commit.mjs
        working-directory: ./${{ matrix.repo }}
        env:
          PERSONAL_GITHUB_TOKEN: ${{ secrets.PERSONAL_GITHUB_TOKEN }}
          REPO: ${{ matrix.repo }}
          BRANCH: ${{ matrix.branch }}
          PRETTIER: ${{ matrix.prettier }}
